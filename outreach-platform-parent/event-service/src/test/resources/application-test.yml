spring:
  # Disable service discovery in tests
  cloud:
    discovery:
      enabled: false

  # Disable security auto-configuration for migration tests
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration

  # JPA - validate schema created by Liquibase
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  # Liquibase enabled (Testcontainers provides datasource)
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

  # MongoDB auto-index creation
  data:
    mongodb:
      auto-index-creation: true

eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
