package com.outreach.platform.event.model;

/**
 * Represents the type of resource that can have fine-grained permissions.
 */
public enum ResourceType {
    SEQUENCE,
    TEMPLATE,
    CONTACT_LIST
}
