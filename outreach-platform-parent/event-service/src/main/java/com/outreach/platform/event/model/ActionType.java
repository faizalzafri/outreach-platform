package com.outreach.platform.event.model;

/**
 * Enum representing the types of actions recorded in the activity feed.
 */
public enum ActionType {
    CREATED,
    MODIFIED,
    SHARED,
    MEMBER_ADDED,
    MEMBER_REMOVED,
    ACTIVATED
}
